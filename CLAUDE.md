# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト

人数・シフト数・日数の3項目だけを入力して、1ヶ月シフトの「たたき台」を出すクライアントサイド完結の Web ツール。公開先は https://kwrkb.github.io/schedule-gen/ 。

**ランタイム依存ゼロは設計上の制約**。フレームワーク・UI ライブラリ・制約ソルバー・LLM のいずれも導入しない。TypeScript / Vite / Vitest はビルド時のみ。依存を増やす提案はまず `VISION.md` と照合すること。

## コマンド

```sh
npm run dev                          # 開発サーバー（Vite）
npm test                             # Vitest（13ケース × 5シード。25秒ほどかかる）
npm run typecheck                    # tsc --noEmit
npm run build                        # typecheck + vite build → dist/

npx vitest run -t '8人'              # テスト名でフィルタ（describe.each のケース名は「8人 × 3 シフト × 30日」形式）
npx vitest run src/solver/solver.test.ts
```

`master` への push で GitHub Actions が `npm test` → `npm run build` → Pages デプロイまで実行する。

## アーキテクチャ

```
src/solver/rng.ts       mulberry32。seed 固定の決定的 PRNG
src/solver/solver.ts    単発の焼きなまし（Solver クラス）+ stats() による検証
src/solver/generate.ts  多スタート。時間予算内で解き直し最良解を採る
src/main.ts             DOM 直書きの UI。表描画と TSV コピー
proto/prototype_shift.py  移植元の Python 参照実装
```

### 中核: 列構成固定方式

1日分の「列」（早1人、中1人、…、遅1人、残りは休み）を `buildColumn()` で作り、全日にコピーしたものが初期解。探索は**同じ日の2人のセルを交換するだけ**なので列構成が壊れず、「各シフトに毎日1人以上」（cover 制約）が探索中つねに満たされる。出勤日数も原理的に均等化される。

この不変条件を壊す変更（セルの単独書き換え、日ごとの出勤人数の変動）は、cover 制約の保証と均等性を同時に失わせる。

### 差分評価

コストは `rowCost()` で1行ずつ計算する。交換は2行しか変化しないため全体再計算は不要。この前提があるので、**行をまたぐコスト項（人と人の間の均等化など）は追加できない** — 入れると差分評価が成立しなくなる。

### セル値の規約

`0` = 休み（`REST`）、`1..S` が勤務で **値の大小がそのまま勤務時間帯の順序**。`1` が早番、`S` が遅番。勤務間インターバルの判定（`violatesInterval`）はこの順序性に直接依存している。

### シフト数の上限は入力値ではなく計算値

`maxShiftsFor(P, D)` が「人数の8割」と「連勤上限を守れる休み枠」の小さい方を返す。UI の `shifts` 入力の `max` はこれに追随する（`main.ts` の `syncShiftsMax`）。**上限を固定値に戻さないこと** — 8人×8シフトのような入力は、休み枠が消えて全員31日連勤という壊れた表を生む。`feasible()` がこれを弾いている。

### 制約はハードとソフトで役割が違う

ハード（`violations` に数える）は cover / 連勤上限 / 勤務間インターバルの3つだけ。遅番の連続は**あえてハードにせず**重み（`W_SAME3` / `W_LATERUN`）で抑えている — 人数が少ない構成でハード化すると解が見つからなくなるため。重みを触ったら20シードで遅番連続が3日以内に収まることを確認する。

### 多スタートが必要な理由

単発の焼きなましはハード制約違反ゼロを保証しない（5人×2シフト×30日で 1〜2% の確率で連勤上限違反）。反復数を増やしても抜けられない局所最適で、Python プロトタイプでも同率で発生する。`generate()` が時間予算 800ms・最大12回で解き直し、**違反数の少なさ → スコアの低さ**の順で採用する。

### 収束判定

冷却完了前（`it >= coolIters` になる前）は打ち切らない。高温中の「改善が止まった」は誤判定になる。冷却完了時に stale をリセットする変更は試して効果なしと確認済み（`LESSONS.md`）。

## テストの性質

Python プロトタイプと同一の乱数列は再現できないため、**出力の一致ではなく性質で検証**する: ハード制約違反 0、出勤日数の幅（ケースごとの許容値）、遅番回数の幅 ≤1回、遅番連続 ≤3日、cover 全日充足、実行時間 < 2840ms。ソルバーに手を入れたらこの13ケース × 5シードが全部通ることを確認する。

出勤日数の幅1は理論限界（延べ出勤枠が人数で割り切れないため）。ここを 0 にしようとしない。シフト数が人数の8割に近いケース（10人×8、25人×20、50人×40）は休み枠が細って幅が2〜4日に広がるので、`CASES` の `work` にケースごとの許容値を持たせている。

## ドキュメント

矛盾時の優先順は **VISION.md > PLAN.md > コード > NOTES.md**。

- `VISION.md` — 正典。目的・スコープ・非スコープ・アルゴリズム仕様。更新はユーザー承認必須
- `PLAN.md` — フェーズ境界と現在地のみ
- `LESSONS.md` — 追記専用。却下した案 / 決め手 / 覆す条件。**設計変更を提案する前にここを読む**（Go→WASM、Web Worker、曜日導入、独自ドメインなどは検討済みで却下されている）

コードが `VISION.md` と矛盾していたら、勝手に寄せず作業を止めて確認する。
