# LESSONS

なぜこうなっているか。何を試して捨てたか。追記専用。

## 2026-08-08: 実装言語は TypeScript（Go→WASM を却下）

**却下した案**

- **Go → 標準 WASM ツールチェーン** — 既存の Go スキルが活き、`go test` でソルバーの制約違反ゼロを型付きで検証でき、同じコードから CLI 版も出せる点で有力だった
- **Go → TinyGo WASM** — バイナリを 100〜200KB 程度まで絞れる想定で、上記のサイズ問題を回避する案
- **JS 直書き** — 依存ゼロで最も軽いが、型がない

**決め手**

- `math/rand` と `math` を呼ぶだけのほぼ空の Go WASM を実測したところ **raw 2,033,319 B (1.94MB) / gzip 後 600,183 B (586KB)**。これは Go ランタイム込みの下限であり、実ソルバー本体と `wasm_exec.js`（約16KB）が上乗せされる。VISION の「導入障壁ゼロ」「ちょっと使うツール」と、初回 600KB 超のダウンロードは正面から衝突する
- `tinygo version` → `command not found`。TinyGo は未インストールで、採用すると新規ツールチェーン依存がビルドからホスティングまで波及する。VISION の「外部ライブラリ依存なし」の方針に反する
- 性能面に Go を選ぶ理由はない。Python プロトタイプの最大ケース（50人×4シフト×31日）が 2.84s であり、このワークロードは JS エンジンで十分に収まる
- JS 直書きではなく TS にしたのは、Go を検討した動機の実質が「型で守る／テストで正しさを担保する」であり、TS はそれを**初回ダウンロード増加ゼロのまま**満たすため。TS のツールチェーンはビルド時のみで、成果物は依存ゼロの静的ファイルに落ちる

**覆す条件**

- ソルバーが JS エンジンで実用時間（体感 1〜2s 以内）に収まらないと**実測で**判明した場合
- WASM が必須となる別要件（既存 C/Go ライブラリの再利用など）が発生した場合

## 2026-08-08: ソルバーはメインスレッドで実行する（Web Worker を却下）

**却下した案**

- **Web Worker でソルバーを実行** — 計算中に UI がブロックされるのを構造的に防げる。移植前は最大ケースの所要時間が不明だったため有力に見えた

**決め手**

- TS 移植後に実測したところ、単発の焼きなましは最大ケース（50人×4シフト×31日）で **71ms**（Python プロトタイプの 1130ms に対し約16倍速い）。多スタート込みでも最大 **798ms**
- 1秒未満であれば「計算中…」表示とボタン無効化で十分に許容範囲であり、Worker の分離（メッセージング、ビルド設定、状態管理）に見合わない
- ユーザー判断として「固まるのは UI で許してもらう」方針を確認済み

**覆す条件**

- 入力範囲の拡大や制約追加で、実測が体感限界（およそ 1s）を超えた場合
- 途中経過の逐次表示やキャンセル機能が要件になった場合（Worker なしでは実現しにくい）

## 2026-08-08: 多スタートで解き直す（単発の焼きなましのままにしない）

**却下した案**

- **単発の焼きなましをそのまま採用** — Python プロトタイプの構成をそのまま移植したもの
- **反復数を増やす** — 局所最適なら低温での探索を増やせば抜けられるはず、という想定
- **冷却完了時に stale カウンタをリセットする** — Python の収束判定は高温中も stale を加算するため、冷却が終わった瞬間に打ち切られ低温の詰めが1ラウンドも走らない。docstring の意図（高温中の誤判定を防ぐ）に照らすとバグに見えた

**決め手**

- 最もタイトな構成（5人×2シフト×30日）で 100 シード試行したところ、単発では **TS 2/100・Python 1/100** の確率で連勤上限違反を含む解を返した。両者に有意差はなく（Fisher 検定 p≈0.24）、移植バグではなく元アルゴリズム固有の性質と判明した。VISION の「ハード制約違反ゼロの解が必ず得られる」はこの構成では成立しない
- 同構成で出勤日数・遅番回数の幅が 2 になるシードが 10 件中 2 件あった（理論限界は 0。5人・30日で各人 24勤務／12遅番が達成可能）
- stale リセットを試すと反復数は 30000→44000 に増えたが、失敗シード（1, 5）の結果は変わらなかった。**反復を増やしても抜けられない**ことが確認できたため revert した
- 時間予算内で解き直し、違反数の少なさ→スコアの低さの順で最良解を採ると、**全ケースで違反 0/100、5人ケースの出勤幅・遅番幅は全 100 シードで 0**、最大所要 798ms。単発が 12〜71ms と十分速いため、予算 800ms で平均 12 回の試行が回る

**覆す条件**

- 入力範囲が広がり、800ms の予算で試行回数が確保できなくなった場合（予算配分か打ち切り条件の見直しが必要）
- コスト関数に人間どうしの出勤日数を直接均等化する項を入れるなど、単発でも局所最適に落ちない設計に変えた場合

## 2026-08-08: 日ごとの出勤人数は固定のままにする（曜日導入・端数吸収を却下）

**却下した案**

- **年月入力で曜日を導入する** — 「日数」を「年月」に置き換えれば入力項目数は3のまま曜日が確定する。月曜は厚く、火水は薄くという需要パターンを表現でき、同時に端数も吸収できた
- **曜日なしで日ごとの出勤人数を±1変動させる** — 端数だけを吸収して出勤日数を完全に均等化する案。入力は増えず VISION とも矛盾しない

**決め手**

- 端数は実在する。延べ出勤が人数で割り切れないケースが多く、8人×3シフト×30日は 180÷8 = **22.5日**、20人×3シフト×31日は 434÷20 = **21.7日**。毎日の出勤人数を固定している限り**出勤幅1は構造的に不可避**
- ただしユーザー確認により、**実務ではシフトごとの必要人数がそもそも一様ではない**ため、差1は手直しの対象にならないと判断された
- VISION の前提「手直しは絶対に発生する」に照らすと、ここを完全均等にしても手直し工数は減らない。入力を増やさずに済む案（±1変動）ですら、コストに見合う効果がない

**覆す条件**

- 利用者から出勤日数の不公平が実際に指摘された場合
- シフトごとの必要人数を指定する機能を入れる場合（そのとき日ごとの人数変動は自然に必要になる）

## 2026-08-08: 独自ドメインを使わない（ブログのサブドメイン接続を却下）

**却下した案**

- **ブログのサブドメインに載せる**（例: `shift.kwrkb.com`）— ブログとの相互導線を張る計画があり、同一ドメインなら回遊が自然になる。ブランドの一貫性でも有利だった

**決め手**

- ブログのドメインは ConoHa WING 管理で、GitHub Pages に向けるには DNS レコードの追加と Pages 側のカスタムドメイン設定・証明書発行を挟む必要がある。**サーバー代ゼロ・導入障壁ゼロ**という VISION の方針に対し、運用対象が1つ増えるだけの変更になる
- 導線は記事本文からのリンクで足り、ドメインが一致している必要はない
- `kwrkb.github.io/schedule-gen/` で公開済みであり、URL を変えると既存リンクの張り替えが発生する

**覆す条件**

- ツール単体で集客する方針に変わり、ドメインが SEO・信頼性の実害になった場合
- ブログ側と共通の解析・認証が必要になり、同一オリジンが要件化した場合
