# PLAN

フェーズ境界と現在地のみ。手順分解は TodoList 側で持つ。

**現在地: Phase 3（公開済み / ブログ導線が残り）**

---

## Phase 0: 足場（完了）

- [x] `git init` + `gh repo create`（private）
- [x] Vite + TypeScript（strict）でプロジェクト初期化。ランタイム依存は入れない
- [x] テスト基盤を用意（`vitest`）
- [x] `prototype_shift.py` を `proto/` へ移動し、移植元の参照実装として残す

**完了条件**: `npm run dev` / `npm run build` / `npm test` が通り、ビルド成果物にランタイム依存が含まれない

---

## Phase 1: ソルバー移植（完了）

- [x] `prototype_shift.py` を TypeScript へ**忠実に**移植する（`Solver`）
- [x] 乱数を seed 固定の決定的な PRNG に置き換える（mulberry32）
- [x] 検証テストを書く（7ケース × 5シード）
- [x] 単発 SA の局所最適に対処するため、時間予算内の多スタートを `generate()` として追加

**完了条件**: VISION 記載の7ケース全てで、自動テストが以下を確認する

- ハード制約違反 **0**（cover / 連勤上限 / 遅番→翌日早番）
- 出勤日数の幅 **≤1日**、遅番回数の幅 **≤1回**
- 実行時間が Python プロトタイプ（最大 2.84s）を下回る

---

## Phase 2: UI（完了）

- [x] 入力フォーム（人数・シフト数・日数）
- [x] 計算中の表示（ボタンの無効化 + 「計算中…」）。ソルバーはメインスレッドで回す
- [x] シフト表（人 × 日）のレンダリング
- [x] Excel へのコピペ導線
- [x] 解なし時の表示

**完了条件**: ブラウザで入力から表出力まで一気通貫で動作し、最大ケースでも「計算中」が見えたまま結果まで到達する（無反応に見えない）

---

## Phase 3: 公開

- [x] ホスティング先を決定 — GitHub Pages。独自ドメインは使わない
- [x] デプロイ — https://kwrkb.github.io/schedule-gen/
- [ ] ブログ記事との相互導線

**完了条件**: 公開URLで第三者がアクセスして使える
