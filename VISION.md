# VISION: シフトたたき台ジェネレーター（仮）

## 一行で言うと

**シフト数と人数を入れるだけで、それなりに人道的な1ヶ月シフトの「たたき台」を出すWebツール。**

---

## なぜ作るのか

### 課題

シフト作成は多くの現場で属人化し、Excelと手作業で回っている。既存のシフト管理SaaS（Airシフト、シフオプ、ジョブカン等）は存在するが、以下の理由で「ちょっと使う」に至らない。

- **初期設定が重い** — 従業員登録、シフトパターン定義、店舗設定、制約設定…と、使い始めるまでが遠い
- **乗り換えコストが高い** — 既存フローを捨てる意思決定が要る
- **オーバースペック** — 「今月のたたき台がほしいだけ」の人には機能が過剰

一方、Excelソルバーは重く、Google OR-Tools のような制約ソルバーは非エンジニアには扱えない。「連勤は6日まで」「遅番の翌日に早番はさせない」といった当たり前の配慮を機械的に効かせたいだけなのに、その手段がない。

### 着眼点

**シフト作成において、最終的に人間の手直しは絶対に発生する。** どんなに高機能なツールでも、個別事情（あの人とあの人は組ませない、来週は研修がある）は人間しか知らない。

ならば、ツールは**完璧を目指すのではなく「手直しの起点」を最速で出す**ことに徹すればいい。ゼロから組むより、たたき台を直す方が圧倒的に楽だからだ。

---

## コンセプト

### ポジション

> **完成品ではなく、たたき台。**

出力されたシフトはExcelに貼り付けて手直しする前提。ツールは骨組みだけを提供する。

### 設計思想

**「入力を極限まで削り、その分を裏側の制約設計に注ぎ込む」**

ユーザーが入力するのは2つだけ。

- シフト数
- 人数

その代わり、裏側では「シフトあるある」を暗黙のうちに全部効かせる。ユーザーは何も設定しないのに、出てくるシフトはちゃんと連勤を避け、遅番の翌朝を避け、負担が偏らない。**この「裏でしっかり考えている」部分こそがプロダクトの本体。**

### ターゲット

**「何も考えたくない人」。**

細かく制約を設定したい人は対象外。そういう人には「ChatGPT/Claudeに直接頼む方法」を解説するブログ記事へ誘導する（別途執筆）。ツール側で複雑さを引き受けない。

---

## 仕様

### 入力

| 項目 | 範囲 |
|---|---|
| 人数 | 1〜50人 |
| シフト数 | 1〜（早番・中番・遅番の種類数） |
| 日数 | 28〜31日（月の日数） |

### シフトラベル

システムが自動命名する。**順序性を持たせることが重要**（勤務間インターバル判定に使うため）。

- 1種類: `日勤`
- 2種類: `早` `遅`
- 3種類以上: `早` `中1` `中2` … `遅`

ユーザーによるリネームは提供しない。Excelで置換すれば済むため、ツールの責任範囲外とする。

### 出力

HTML形式のシフト表（人 × 日のマトリクス）。Excelへコピペできる形が望ましい。

### 制約（すべて自動適用・ユーザー設定不可）

**ハード制約（必ず守る）**

- 連勤上限6日
- 各シフトに最低1人配置
- 遅番の翌日に早番を入れない（勤務間インターバル11時間ルールの近似）

**ソフト制約（できるだけ均等に）**

- 出勤日数を全員均等に（週休2日相当）
- 遅番回数を全員均等に
- 休みが飛び石で自然に散る（固まりすぎない）
- 同一シフトが3日以上続かない

### 意図的にやらないこと

| 項目 | 理由 |
|---|---|
| 休み希望の入力 | 入力の手間がリピート利用の壁になる。手直しで対応する領域 |
| 定休日の設定 | 同上 |
| 土日祝の考慮 | 土日休みの公平化は年単位でしか成立しない。月次ツールの守備範囲外 |
| データの保存 | 入力が2項目なので保存する価値がない。プライバシー面でも有利 |
| ユーザー登録 | 導入障壁をゼロにする |
| LLMの利用 | APIコスト・精度リスクを排除。古典的アルゴリズムで十分 |
| 複雑な制約設定 | ブログ記事でChatGPT/Claude直接利用へ誘導 |

---

## アルゴリズム（検証済み）

**焼きなまし法（Simulated Annealing）による交換ベース局所探索**を、時間予算の範囲で複数回走らせて最良解を採る。

### 中核アイデア：列構成固定方式

1. **1日分の「列」を作る** — 早1人、中1人、…、遅1人、残りは休み。週休2日相当になるよう休みの人数を決める
2. **その同じ列を全日にコピー** — 初期状態は「P1は毎日早番、P2は毎日中番、…、Pnは毎日休み」という極端に偏った状態
3. **同じ日の2人を交換して改善していく** — 交換なので列構成は絶対に壊れない

この方式の利点：

- **cover制約（各シフト最低1人）が構造的に常に満たされる** — 探索中に壊れようがない
- **出勤日数が原理的に均等化できる** — 全員が同じ列を共有するため

> 補足: 初期解をランダム配分にする方式も試したが、出勤日数のばらつきが最後まで解消されず（50人ケースで10日差）、列固定方式が明確に優れていた。

### 評価関数

行（個人）単位でコストを計算し、**差分評価**で高速化する。交換は2行しか変化しないため、全体再計算が不要。

```
コスト = ハード制約違反 × 大きな重み
       + 各種分散（シフト構成・休み間隔） × 小さな重み
```

| 項目 | 重み |
|---|---|
| 連勤超過 | 5000 |
| 遅番→翌日早番 | 3000 |
| シフト構成の偏り（分散） | 40 |
| 休み間隔の分散 | 8 |
| 同一シフト3連続 | 4 |

### 収束判定

時間固定ではなく**早期終了**を採用。

- 1ラウンド = P×D 回の交換試行
- 8ラウンド連続で改善率が0.1%未満なら打ち切り
- **ただし冷却完了前は収束判定を行わない**（高温中の「改善停止」は誤判定になる）

温度スケジュールは反復数連動、冷却しきる反復数は問題規模（P×D×60）に比例させる。

### 多スタート

**単発の焼きなましはハード制約違反ゼロを保証しない。** 最もタイトな構成（5人×2シフト×30日）では、
100シード中1〜2件で連勤上限違反を含む解に落ちる。これは反復数を増やしても抜けられない局所最適であり、
Python プロトタイプでも同率で発生する元アルゴリズム固有の性質である。

そこで時間予算（既定 800ms、最大12回）の範囲で解き直し、
**ハード制約違反の少なさを最優先し、同数ならコストの低い解**を採用する。
これにより全ケースで違反ゼロになる。

### 検証結果

TypeScript 実装を各ケース20シードで実行した実測値（最悪値）。

| ケース | 時間 | 出勤日数の幅 | 遅番回数の幅 | 違反 |
|---|---|---|---|---|
| 8人×3シフト×30日 | 0.23s | 1日 | 1回 | 0 |
| 12人×4シフト×30日 | 0.19s | 1日 | 0回 | 0 |
| 20人×3シフト×31日 | 0.45s | 1日 | 1回 | 0 |
| 50人×4シフト×31日 | 0.82s | 1日 | 1回 | 0 |
| 5人×2シフト×30日 | 0.10s | 0日 | 0回 | 0 |
| 30人×5シフト×31日 | 0.47s | 1日 | 1回 | 0 |
| 6人×2シフト×28日 | 0.19s | 1日 | 1回 | 0 |

人数がシフト数で割り切れない限り完全均等は不可能なため、**差1はほぼ理論限界**。
実務ではシフトごとの必要人数がそもそも一様ではないため、この程度のズレは手直しの対象にならない。

### 解なし判定

探索前に数学的に判定する。

- 人数 < シフト数 → 全シフトを埋められない
- その他、週休2日相当が確保できないケース

該当時は「見つかりませんでした」と表示するのみ。凝ったフィードバックは提供しない。

---

## 技術方針

### 決定事項

- **クライアントサイド完結**（サーバー代ゼロ、データが外部に出ない）
- 静的ホスティング — **GitHub Pages**（master への push で自動デプロイ）
- **実装言語は TypeScript** — ビルド時のみツールチェーンを使い、成果物は依存ゼロの静的ファイル
- **ランタイム依存はゼロ** — フレームワーク・UIライブラリ・制約ソルバーのいずれも使わない
- ソルバーはメインスレッドで実行する。計算中は進行中であることを表示し、待ち時間は UI の見せ方で許容してもらう
- 独自ドメインは使わない。`kwrkb.github.io/schedule-gen/` のまま運用する

> 参考: OR-Tools の WASM 版は有志プロジェクトとして存在するが、公式サポート外（Emscripten対応は優先度ゼロと公式が明言）。今回のアルゴリズムでは不要。

---

## マネタイズ

**当面なし。** サーバー代がかからない設計にすることで持ち出しを最小化する。

ブログ（既存ドメイン）との連携で、記事流入とツール利用を相互に回すことを想定。

---

## 成功の定義

1. **叩き台が出れば上等** — 完成度より、まず動くものを公開する
2. 使った人が「ゼロから組むよりマシ」と思えば成功
3. リピート利用されればなお良いが、必須ではない

---

## 参考: Python プロトタイプ

`proto/prototype_shift.py` にアルゴリズムの原型を残してある。TypeScript 実装（`src/solver/`）は
このロジックを踏襲しており、単発の焼きなましは `Solver`、多スタートは `generate()` に分かれている。
